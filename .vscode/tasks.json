{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run main",
            "type": "process",
            "command": "${command:python.interpreterPath}",
            "args": [
                "${workspaceFolder}/src/main.py"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "env": {
                    "PYTHONPATH": "."
                }
            }
        }
    ]
}